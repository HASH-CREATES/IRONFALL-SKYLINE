{
  "name": "IRONFALL-skyline",
  "version": "1.0.0",
  "description": "IRONFALL // SKYLINE — browser armored-superhero flight game (original IP, gesture + keyboard control)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 8000"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.14",
    "three": "^0.160.0"
  }
}
